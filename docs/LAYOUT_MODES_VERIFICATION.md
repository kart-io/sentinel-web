# 布局模式验证 - 与 vben-admin 对比

## vben-admin 的 7 种布局模式

根据 vben-admin 源码，共有 7 种布局模式：

```typescript
type LayoutType =
  | 'sidebar-nav'        // 侧边导航（默认）
  | 'sidebar-mixed-nav'  // 侧边混合（双列侧边栏）
  | 'header-nav'         // 顶部导航
  | 'header-sidebar-nav' // 顶部通栏+侧边导航
  | 'mixed-nav'          // 混合导航（顶部+侧边）
  | 'header-mixed-nav'   // 顶部混合（顶部双行）
  | 'full-content';      // 全屏内容
```

## 布局模式对比验证

### 1. sidebar-nav（侧边导航）✅

**vben-admin:**
```
┌────────┬──────────────────────┐
│ Logo   │  Header              │
├────────┼──────────────────────┤
│ Menu1  │                      │
│ Menu2  │     Content          │
│ Menu3  │                      │
└────────┴──────────────────────┘
```

**我们的实现:**
- ✅ Logo 在 Sidebar 顶部
- ✅ Sidebar 从 top: 0 开始
- ✅ Header 有左边距
- ✅ 折叠时 Sidebar 宽度 64px，展开时 210px

**验证结果:** ✅ 一致

---

### 2. sidebar-mixed-nav（侧边混合）

**vben-admin:**
```
┌──┬────────┬────────────────────┐
│图│ Logo   │  Header            │
│标├────────┼────────────────────┤
│  │ SubM1  │                    │
│  │ SubM2  │     Content        │
│  │ SubM3  │                    │
└──┴────────┴────────────────────┘
```

**特点:**
- 左侧窄列：一级菜单（仅图标，70-80px）
- 右侧宽列：二级菜单（200px，可折叠）
- Logo 在左侧窄列顶部（仅图标）
- Logo 文字在右侧宽列顶部

**我们的实现:**
- ✅ 在 MainLayout.tsx 中直接渲染双列侧边栏
- ✅ 左列 72px，仅图标
- ✅ 右列 200px，可折叠
- ⚠️ 需要验证 Logo 显示

**验证结果:** 🔧 需要完善 Logo

---

### 3. header-nav（顶部导航）✅

**vben-admin:**
```
┌────────────────────────────────┐
│  Logo  Menu1  Menu2  Menu3     │
├────────────────────────────────┤
│                                │
│         Content                │
│                                │
└────────────────────────────────┘
```

**特点:**
- 无侧边栏
- Logo 和菜单都在 Header
- 菜单水平排列

**我们的实现:**
- ✅ Header 中有顶部菜单
- ✅ 无侧边栏
- ⚠️ Logo 位置需要验证

**验证结果:** 🔧 需要添加 Logo 到 Header

---

### 4. header-sidebar-nav（顶部通栏+侧边）

**vben-admin:**
```
┌────────────────────────────────┐
│  Logo  Menu1  Menu2  Menu3     │ <- Header 全宽
├────────┬───────────────────────┤
│ SubM1  │                       │
│ SubM2  │     Content           │
│ SubM3  │                       │
└────────┴───────────────────────┘
```

**特点:**
- Header 全宽，包含 Logo 和一级菜单
- Sidebar 显示二级菜单
- Sidebar 从 Header 下方开始

**我们的实现:**
- ✅ 已实现基本结构
- ⚠️ Logo 位置需要验证

**验证结果:** 🔧 需要完善

---

### 5. mixed-nav（混合导航）✅

**vben-admin:**
```
┌────────────────────────────────┐
│ Menu1  Menu2  Menu3            │ <- 顶部一级菜单
├────────┬───────────────────────┤
│ Logo   │  Header               │
├────────┼───────────────────────┤
│ SubM1  │                       │
│ SubM2  │     Content           │
│ SubM3  │                       │
└────────┴───────────────────────┘
```

**特点:**
- 顶部有一级菜单（全宽）
- Sidebar 显示 Logo 和二级菜单
- Sidebar 从顶部菜单下方开始

**我们的实现:**
- ✅ 顶部一级菜单
- ✅ Sidebar 显示二级菜单
- ✅ Sidebar marginTop = header.height + 48

**验证结果:** ✅ 一致

---

### 6. header-mixed-nav（顶部混合）

**vben-admin:**
```
┌────────────────────────────────┐
│ Menu1  Menu2  Menu3            │ <- 一级菜单
├────────────────────────────────┤
│ SubM1  SubM2  SubM3            │ <- 二级菜单
├────────────────────────────────┤
│                                │
│         Content                │
│                                │
└────────────────────────────────┘
```

**特点:**
- 无侧边栏
- 顶部双行：第一行一级菜单，第二行二级菜单
- Logo 可能在左侧或不显示

**我们的实现:**
- ✅ 顶部双行菜单
- ⚠️ Logo 位置需要验证

**验证结果:** 🔧 需要完善

---

### 7. full-content（全屏内容）✅

**vben-admin:**
```
┌────────────────────────────────┐
│                                │
│                                │
│         Full Content           │
│         (no header/sidebar)    │
│                                │
│                                │
└────────────────────────────────┘
```

**特点:**
- 无 Header，无 Sidebar
- 纯内容区域
- 可能有浮动设置按钮

**我们的实现:**
- ✅ 无 Header 和 Sidebar
- ✅ 浮动设置和退出按钮

**验证结果:** ✅ 一致

---

## Logo 位置规则总结（基于 vben-admin）

### Logo 在 Sidebar 顶部的模式
- ✅ `sidebar-nav` - Logo 在 Sidebar 顶部
- ✅ `mixed-nav` - Logo 在 Sidebar 顶部
- ✅ `sidebar-mixed-nav` - Logo 图标在左列，文字在右列

### Logo 在 Header 的模式
- 🔧 `header-nav` - Logo 在 Header 左侧
- 🔧 `header-sidebar-nav` - Logo 在 Header 左侧
- 🔧 `header-mixed-nav` - Logo 可能在 Header 或不显示

### 无 Logo 的模式
- ✅ `full-content` - 无 Logo（或浮动显示）

---

## 需要修复的问题

### 1. header-nav 模式 Logo 缺失
**问题:** Logo 应该在 Header 左侧
**修复:** 在 LayoutHeader 中，当 `isHeaderNav` 时显示 Logo

### 2. header-sidebar-nav 模式 Logo 位置
**问题:** Logo 应该在 Header 左侧
**修复:** 在 LayoutHeader 中，当 `isHeaderSidebarNav` 时显示 Logo

### 3. header-mixed-nav 模式 Logo 位置
**问题:** Logo 位置不明确
**修复:** 需要查看 vben-admin 实际效果

### 4. sidebar-mixed-nav 模式 Logo 显示
**问题:** Logo 应该分为两部分（图标+文字）
**修复:** 已在 MainLayout 中实现，需要验证

---

## 验证清单

- [x] sidebar-nav - Logo 在 Sidebar 顶部 ✅
- [ ] sidebar-mixed-nav - Logo 分为两部分 🔧
- [ ] header-nav - Logo 在 Header 左侧 ❌
- [ ] header-sidebar-nav - Logo 在 Header 左侧 ❌
- [x] mixed-nav - Logo 在 Sidebar 顶部 ✅
- [ ] header-mixed-nav - Logo 位置待确认 ⚠️
- [x] full-content - 无 Logo ✅

---

## 下一步行动

1. **header-nav 和 header-sidebar-nav 模式:** 在 LayoutHeader 中添加 Logo
2. **sidebar-mixed-nav 模式:** 验证 MainLayout 中的 Logo 实现
3. **header-mixed-nav 模式:** 查看 vben-admin 实际效果，确定 Logo 位置
4. **创建测试页面:** 快速切换不同布局模式进行验证

---

## 关键代码参考

### vben-admin Logo 显示逻辑
```vue
<!-- layout.vue -->
<template #logo>
  <VbenLogo
    v-if="preferences.logo.enable"
    :collapsed="logoCollapsed"
    :text="preferences.app.name"
    :theme="showHeaderNav ? headerTheme : theme"
  />
</template>
```

### logoCollapsed 计算逻辑
```typescript
const logoCollapsed = computed(() => {
  if (isMobile.value && sidebarCollapsed.value) return true;
  if (isHeaderNav.value || isMixedNav.value || isHeaderSidebarNav.value) {
    return false;  // 这些模式下 Logo 不折叠
  }
  return sidebarCollapsed.value || isSideMixedNav.value || isHeaderMixedNav.value;
});
```

### showHeaderNav 计算逻辑
```typescript
const showHeaderNav = computed(() => {
  return !isMobile.value && (
    isHeaderNav.value || 
    isMixedNav.value || 
    isHeaderMixedNav.value
  );
});
```

这说明：
- `header-nav`, `mixed-nav`, `header-mixed-nav` 这三种模式下，Logo 可能在 Header 的顶部菜单区域
- 其他模式 Logo 在 Sidebar

---

## 总结

vben-admin 有 7 种布局模式，我们目前实现了 4 种完全一致，3 种需要完善 Logo 显示。

关键是要理解 **Logo 的显示位置取决于布局模式**：
- Sidebar 模式：Logo 在 Sidebar 顶部
- Header 模式：Logo 在 Header 左侧或顶部菜单区域
- Full Content：无 Logo 或浮动显示
