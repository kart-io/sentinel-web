# 本地环境配置示例
# 复制此文件为 .env.local 并修改配置
# .env.local 不会被 Git 跟踪，可以存放敏感信息

# 应用基本信息
VITE_APP_TITLE=Sentinel-X Web (本地)
VITE_APP_VERSION=0.0.0

# API 配置
VITE_API_BASE_URL=http://localhost:3000/api
VITE_API_TIMEOUT=30000

# 环境标识
VITE_APP_ENV=local

# 调试配置
VITE_ENABLE_MOCK=true
VITE_ENABLE_DEBUG=true

# 日志级别
VITE_LOG_LEVEL=debug

# Sentry 配置
# 从 Sentry 项目设置中获取 DSN
# 格式: https://<key>@<organization>.ingest.sentry.io/<project>
VITE_SENTRY_DSN=
VITE_SENTRY_ENABLED=false

# 加密存储配置
# 是否启用 localStorage 加密（生产环境推荐启用）
VITE_ENABLE_STORAGE_ENCRYPTION=false
# 加密密钥（生产环境必须设置强密钥，建议至少 32 位）
# 可以使用 openssl rand -base64 32 生成随机密钥
VITE_ENCRYPTION_KEY=

# 示例：其他可能的配置
# VITE_AUTH_TOKEN=your-token-here
# VITE_API_KEY=your-api-key-here
