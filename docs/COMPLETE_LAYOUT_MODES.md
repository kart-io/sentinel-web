# 完整布局模式实现文档

## 📋 概述

本文档详细说明了 Sentinel Web 项目中实现的 7 种布局模式，这些布局模式参考了 Vben Admin 的设计，提供了灵活多样的页面布局方案。

## 🎯 实现状态

| 布局模式 | 状态 | 说明 |
|---------|------|------|
| sidebar-nav | ✅ 完整 | 默认模式,左侧垂直菜单 |
| header-nav | ✅ 完整 | 顶部横向菜单 |
| mixed-nav | ✅ 完整 | 顶部一级菜单+侧边二级菜单 |
| sidebar-mixed-nav | ✅ 完整 | 双列侧边栏(左窄右宽) |
| header-mixed-nav | ✅ 完整 | 顶部双行菜单 |
| header-sidebar-nav | ✅ 完整 | Header 全宽+侧边栏 |
| full-content | ✅ 完整 | 纯内容展示 |

## 📂 核心文件

### 1. 菜单配置
**文件**: `/src/config/menuConfig.tsx`

这个文件定义了支持多级菜单的完整配置：

```typescript
// 核心接口
interface MenuItem {
  key: string;
  label: string;
  icon?: React.ReactNode;
  path?: string;
  children?: MenuItem[];
  badge?: number | string;
}

// 工具函数
- convertToAntdMenu(): 转换为 Ant Design Menu 格式
- getTopLevelMenu(): 获取一级菜单(用于 mixed-nav)
- getSubMenu(): 根据父级 key 获取二级菜单
- getAllPathsMap(): 获取所有路径映射
- getParentKey(): 根据路径获取父级菜单 key
- getFlatMenuItems(): 获取扁平化的所有菜单项
```

### 2. 主布局组件
**文件**: `/src/layouts/MainLayout.tsx`

这是核心布局组件，实现了所有 7 种布局模式的切换逻辑。

### 3. 布局状态管理
**文件**: `/src/store/layoutStore.ts`

使用 Zustand 管理全局布局状态，包括：
- 7 种布局类型的定义
- 侧边栏、Header、Tabbar、Footer 等配置
- 主题、过渡动画等设置
- `useLayoutComputed` 钩子提供计算属性

### 4. 布局测试页面
**文件**: `/src/features/layout-demo/LayoutTestPage.tsx`

提供可视化的布局测试和切换界面。

## 🎨 布局模式详解

### 1. sidebar-nav (侧边导航布局)

**特点**:
- 左侧垂直菜单
- 支持折叠/展开
- 支持悬停展开
- 固定定位

**使用场景**:
- 默认推荐布局
- 适合菜单项较多的应用
- 经典后台管理系统布局

**布局结构**:
```
┌────────────────────────────────┐
│         Header (全宽)          │
├──────┬─────────────────────────┤
│      │                         │
│ Side │      Main Content       │
│ bar  │                         │
│      │                         │
└──────┴─────────────────────────┘
```

### 2. header-nav (顶部导航布局)

**特点**:
- 顶部横向菜单
- 全宽布局
- 响应式设计
- 支持自动隐藏

**使用场景**:
- 菜单项较少的应用
- 需要最大化内容区域
- 宽屏优先的应用

**布局结构**:
```
┌────────────────────────────────┐
│         Header                 │
├────────────────────────────────┤
│      横向菜单 (Menu)            │
├────────────────────────────────┤
│                                │
│        Main Content            │
│                                │
└────────────────────────────────┘
```

### 3. mixed-nav (混合导航布局)

**特点**:
- 顶部显示一级菜单
- 侧边显示二级菜单
- 动态联动切换
- 两级菜单结构清晰

**使用场景**:
- 菜单层级较深的应用
- 需要区分模块和功能
- 大型企业级应用

**布局结构**:
```
┌────────────────────────────────┐
│         Header                 │
├────────────────────────────────┤
│   一级菜单 (横向)               │
├──────┬─────────────────────────┤
│ 二级 │                         │
│ 菜单 │      Main Content       │
│(侧边)│                         │
└──────┴─────────────────────────┘
```

**实现要点**:
```typescript
// 1. 状态管理：追踪当前选中的一级菜单
const [selectedTopMenu, setSelectedTopMenu] = useState<string>('dashboard');

// 2. 根据选中的一级菜单动态获取二级菜单
const sidebarMenuItems = getSubMenu(selectedTopMenu, menuConfig);

// 3. 顶部菜单点击时切换侧边栏内容
const handleTopMenuClick = (e) => {
  setSelectedTopMenu(e.key);
};
```

### 4. sidebar-mixed-nav (侧边混合布局)

**特点**:
- 双列侧边栏
- 左列显示一级菜单(仅图标)
- 右列显示二级菜单
- 空间利用高效

**使用场景**:
- 需要快速切换模块
- 空间有限但菜单较多
- 图标化导航设计

**布局结构**:
```
┌────────────────────────────────┐
│         Header (全宽)          │
├──┬───┬─────────────────────────┤
│图│二 │                         │
│标│级 │      Main Content       │
│列│菜 │                         │
│  │单 │                         │
└──┴───┴─────────────────────────┘
```

**实现要点**:
```typescript
// 左侧窄列 (72px): 仅图标的一级菜单
<Sider width={72}>
  <Menu
    items={topLevelMenuItems}
    onClick={handleSidebarTopMenuClick}
    // 隐藏菜单标题，仅显示图标
    className="[&_.ant-menu-title-content]:hidden"
  />
</Sider>

// 右侧宽列 (200px): 当前选中的二级菜单
<div style={{ left: '72px', width: '200px' }}>
  <Menu
    items={getSubMenu(selectedTopMenu, menuConfig)}
  />
</div>
```

### 5. header-mixed-nav (顶部混合布局)

**特点**:
- 顶部双行菜单
- 第一行显示一级菜单
- 第二行显示二级菜单
- 无侧边栏，宽屏优化

**使用场景**:
- 宽屏显示器
- 希望顶部集中所有导航
- 最大化左右空间

**布局结构**:
```
┌────────────────────────────────┐
│         Header                 │
├────────────────────────────────┤
│   一级菜单 (横向)               │
├────────────────────────────────┤
│   二级菜单 (横向)               │
├────────────────────────────────┤
│                                │
│        Main Content            │
│                                │
└────────────────────────────────┘
```

**实现要点**:
```typescript
<div className="fixed" style={{ top: `${header.height}px` }}>
  {/* 第一行: 一级菜单 */}
  <Menu
    mode="horizontal"
    items={topLevelMenuItems}
    onClick={handleTopMenuClick}
  />

  {/* 第二行: 二级菜单 */}
  <Menu
    mode="horizontal"
    items={getSubMenu(selectedTopMenu, menuConfig)}
    onClick={handleMenuClick}
  />
</div>
```

### 6. header-sidebar-nav (顶部通栏+侧边布局)

**特点**:
- Header 全宽，不被侧边栏分割
- 侧边栏在 Header 下方
- 独立的区域划分
- 经典企业级布局

**使用场景**:
- 需要统一的顶部品牌区域
- Header 需要放置全局功能
- 传统管理系统布局

**布局结构**:
```
┌────────────────────────────────┐
│    Header (完整全宽)            │
├──────┬─────────────────────────┤
│      │                         │
│ Side │      Main Content       │
│ bar  │                         │
│      │                         │
└──────┴─────────────────────────┘
```

**与 sidebar-nav 的区别**:
- `sidebar-nav`: Header 与 Sidebar 并列，Header 宽度受限
- `header-sidebar-nav`: Header 独立占据全宽，Sidebar 在其下方

### 7. full-content (全屏内容布局)

**特点**:
- 纯内容展示
- 无任何导航元素
- 无边栏、无顶栏
- 沉浸式体验

**使用场景**:
- 数据大屏
- 可视化展示
- 全屏演示
- 登录/注册页面

**布局结构**:
```
┌────────────────────────────────┐
│                                │
│                                │
│        Full Content            │
│                                │
│                                │
└────────────────────────────────┘
```

## 🔧 如何切换布局

### 方法 1: 使用布局测试页面

1. 访问 `/layout-test` 路由
2. 在页面中点击对应的布局卡片
3. 实时预览效果

### 方法 2: 使用偏好设置抽屉

1. 点击右上角设置图标
2. 在"布局配置"部分选择布局类型
3. 配置会自动保存

### 方法 3: 编程方式切换

```typescript
import { useLayoutStore } from '@/store/layoutStore';

function YourComponent() {
  const setLayout = useLayoutStore((state) => state.setLayout);

  // 切换到混合导航布局
  setLayout('mixed-nav');

  // 切换到顶部导航布局
  setLayout('header-nav');
}
```

## 📱 响应式设计

所有布局模式都支持响应式设计：

### 移动端 (<768px)
- 自动折叠侧边栏
- 显示移动端菜单按钮
- 点击遮罩层关闭侧边栏

### 平板 (768px - 1024px)
- 适度调整侧边栏宽度
- 优化触控交互

### 桌面端 (>1024px)
- 完整布局展示
- 支持悬停展开等高级特性

## 🎨 主题系统

支持 3 种主题模式：
- `light`: 亮色主题
- `dark`: 暗色主题
- `system`: 跟随系统

```typescript
const toggleThemeMode = useLayoutStore((state) => state.toggleThemeMode);

// 切换主题
toggleThemeMode();
```

## 💾 配置持久化

所有布局配置自动保存到 localStorage：
- 布局类型
- 侧边栏状态
- 主题模式
- 其他偏好设置

刷新页面后配置保持不变。

## 🔌 扩展性

### 添加新的布局模式

1. 在 `layoutStore.ts` 中添加新的 LayoutType:
```typescript
export type LayoutType =
  | 'sidebar-nav'
  // ... 现有类型
  | 'your-new-layout'; // 新类型
```

2. 在 `useLayoutComputed` 中添加计算属性:
```typescript
const isYourNewLayout = state.app.layout === 'your-new-layout';
```

3. 在 `MainLayout.tsx` 中实现渲染逻辑:
```typescript
{isYourNewLayout && (
  // 你的布局实现
)}
```

### 自定义菜单结构

编辑 `/src/config/menuConfig.tsx`，添加或修改菜单项：

```typescript
export const menuConfig: MenuItem[] = [
  {
    key: 'your-module',
    label: '你的模块',
    icon: <YourIcon size={18} />,
    children: [
      {
        key: '/your-path',
        label: '子菜单',
        path: '/your-path',
      },
    ],
  },
];
```

## 🧪 测试

### 访问测试页面

```
http://localhost:5173/layout-test
```

### 功能测试清单

- [ ] 所有 7 种布局模式可以正常切换
- [ ] 侧边栏折叠/展开功能正常
- [ ] 主题切换功能正常
- [ ] 标签页功能正常
- [ ] 面包屑导航正常
- [ ] 响应式布局正常
- [ ] 配置持久化正常
- [ ] 菜单联动正常(mixed-nav, sidebar-mixed-nav)

## 📸 效果截图

使用测试页面可以快速预览所有布局模式的效果。建议在不同设备和屏幕尺寸下测试。

## 🐛 已知问题

目前没有已知的严重问题。如发现问题，请：
1. 检查浏览器控制台错误
2. 清除 localStorage 重试
3. 查看开发者工具的网络请求

## 📚 参考资料

- [Vben Admin 官方文档](https://doc.vben.pro/)
- [Ant Design Menu 组件](https://ant.design/components/menu-cn)
- [Zustand 状态管理](https://github.com/pmndrs/zustand)

## 🎉 总结

本项目成功实现了 7 种灵活的布局模式，每种模式都有其特定的使用场景：

1. **sidebar-nav**: 默认推荐，经典布局
2. **header-nav**: 简洁横向，适合菜单少
3. **mixed-nav**: 两级联动，适合深层级
4. **sidebar-mixed-nav**: 双列侧边，空间高效
5. **header-mixed-nav**: 双行顶部，宽屏优化
6. **header-sidebar-nav**: 全宽顶部，经典企业
7. **full-content**: 纯内容，沉浸式

所有布局模式都支持：
- ✅ 响应式设计
- ✅ 暗色/亮色主题
- ✅ 配置持久化
- ✅ 平滑过渡动画
- ✅ 多级菜单支持

访问 `/layout-test` 页面可以实时体验和测试所有布局模式！
